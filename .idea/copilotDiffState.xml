<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/headers/common.h">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/headers/common.h" />
              <option name="originalContent" value="#ifndef COMMON_H&#10;#define COMMON_H&#10;&#10;//rozmery sveta&#10;#define MAX_ROWS 50&#10;#define MAX_COLS 50&#10;//stred sveta&#10;#define CENTER_X 0&#10;#define CENTER_Y 0&#10;&#10;//typ policok&#10;typedef enum {&#10;    PRAZDNE, //prazdne policko, moze prejst chodec&#10;    PREKAZKA, //prekazka, ak je svet s prekazkami&#10;    CHODEC //aktualna pozicia chodca&#10;}Typ_policka;&#10;&#10;//typ sveta&#10;typedef enum {&#10;    SVET_BEZ_PREKAZOK, //svet bez prekazok&#10;    SVET_S_PREKAZKAMI //svet s prekazkami&#10;}Typ_sveta;&#10;&#10;//struktura pre poziciu (policko)&#10;typedef struct {&#10;    int riadok;&#10;    int stlpec;&#10;}Pozicia_t;&#10;&#10;//stav simulacie&#10;typedef enum {&#10;    SIM_INIT, //Simulacia inicializovana&#10;    SIM_RUNNING, //prebieha&#10;    SIM_PAUSED, //zastavena&#10;    SIM_FINISHED, //skoncila&#10;    SIM_EXIT //aplikacia sa ukoncuje&#10;}Simulacia_stav;&#10;&#10;#endif" />
              <option name="updatedContent" value="#ifndef COMMON_H&#10;#define COMMON_H&#10;&#10;//rozmery sveta&#10;#define MAX_ROWS 50&#10;#define MAX_COLS 50&#10;//stred sveta&#10;#define CENTER_X 0&#10;#define CENTER_Y 0&#10;&#10;//typ policok&#10;typedef enum {&#10;    PRAZDNE, //prazdne policko, moze prejst chodec&#10;    PREKAZKA, //prekazka, ak je svet s prekazkami&#10;    CHODEC //aktualna pozicia chodca&#10;}Typ_policka;&#10;&#10;//typ sveta&#10;typedef enum {&#10;    SVET_BEZ_PREKAZOK, //svet bez prekazok&#10;    SVET_S_PREKAZKAMI //svet s prekazkami&#10;}Typ_sveta;&#10;&#10;//struktura pre poziciu (policko)&#10;typedef struct {&#10;    int riadok;&#10;    int stlpec;&#10;}Pozicia_t;&#10;&#10;//stav simulacie&#10;typedef enum {&#10;    SIM_INIT, //Simulacia inicializovana&#10;    SIM_RUNNING, //prebieha&#10;    SIM_PAUSED, //zastavena&#10;    SIM_FINISHED, //skoncila&#10;    SIM_EXIT //aplikacia sa ukoncuje&#10;}Simulacia_stav;&#10;&#10;#endif" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>